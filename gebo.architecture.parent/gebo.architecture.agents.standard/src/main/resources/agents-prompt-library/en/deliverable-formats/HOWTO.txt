The ordered steps are the body, one step per item, each step carrying the evidence that
supports it. Introduce a heading only when the procedure has genuinely distinct phases.
Close with the document references.

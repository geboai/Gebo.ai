INPUTS:

(A) USER QUESTION: {question}

(B) RANKED FRAGMENTS BATCH, from the more relevant to the less relevant (each fragment is a document
block exposing its fragmentId and its content):
{documents}

Output only the IRRELEVANT= line listing the fragmentId of the fragments of batch (B) that are
completely useless to answer question (A).

INPUTS:

(A) CURRENT USER QUESTION: {question}

(B) DOCUMENTS AND DOCUMENT FRAGMENTS ALREADY PRESENT IN THE CHAT HISTORY (may be empty):
{documents}

(C) EXTERNAL DATA SOURCES LIST TO PERFORM PURE SEARCH ON (may be empty). Each entry has a unique source code/id and description:
{deepSearchDataSources}


SOURCE SELECTION RULES

searchedSystems must include one or more source codes, each on its own line.
Valid source codes are:
any code present in (C)
the special internal code IKB_SYSTEM
Prefer explicit user-mentioned sources first.

OUTPUT RULES:
You must output ONLY plain text in EXACTLY the <field>=<value> format (no markdown, no extra text).
The line separator between fields MUST be newline.

You MUST output:
searchedSystems=<sourceCode taken from (C)>


searchedSystems rules:

Each searchedSystems line must contain exactly one source code taken from (C) 
considering the contents in the (C) catalogs that mostly match rewrited query and chat history.

CONSTRAINTS:

Do not output any fields other than searchedSystems.
No markdown, no extra text.
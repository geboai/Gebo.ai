spring:
  application:
    # Microservice id: normalized (brain_gebo_ai) it matches the shared topology
    # entry and the models-replication participant set, so this service joins the
    # LLM models-replication cache.
    name: brain.gebo.ai

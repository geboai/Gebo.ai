<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>ai.gebo.apps.microservices</groupId>
		<artifactId>gebo.microservices.apps.parent</artifactId>
		<version>1.0.2.4-SNAPSHOT</version>
	</parent>
	<artifactId>googledrive.gebo.ai</artifactId>

	<!--
	  Google Drive content-handler microservice. The gebo.microservices.starter
	  provides the microservices environment, RabbitMQ bridge, shared topology and
	  (inherited) Eureka discovery client; gebo.googleworkspace.handlers is the hosted
	  content system.
	-->
	<properties>
		<dependency-check.version>12.2.2</dependency-check.version>

		<!-- build ordinaria: non bloccare -->
		<dependency-check.failBuildOnCVSS>11</dependency-check.failBuildOnCVSS>
		<dependency-check.failOnError>false</dependency-check.failOnError>
	</properties>
	<dependencies>
		<dependency>
			<groupId>ai.gebo.microservices.architecture</groupId>
			<artifactId>gebo.microservices.content-handler.starter</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>ai.gebo.systems</groupId>
			<artifactId>gebo.googleworkspace.handlers</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.owasp</groupId>
				<artifactId>dependency-check-maven</artifactId>
				<version>${dependency-check.version}</version>
				<!-- non eseguire questa execution nei singoli moduli -->
				<inherited>false</inherited>
				<executions>
					<execution>
						<id>dependency-vulnerability-check</id>
						<phase>verify</phase>
						<goals>
							<goal>aggregate</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<autoUpdate>true</autoUpdate>
					<failBuildOnCVSS>${dependency-check.failBuildOnCVSS}</failBuildOnCVSS>
					<failOnError>${dependency-check.failOnError}</failOnError>
					<formats>
						<format>HTML</format>
						<format>JSON</format>
					</formats>
					<outputDirectory>${project.build.directory}/dependency-check</outputDirectory>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<profiles>
		<!-- Bootable fat jar (classifier bootable). -->
		<!-- Bootable fat jar WITH the OpenAPI/Swagger UI (classifier
		bootable+swagger). Off by default: production ships no API console. It is
		also what lets this service publish /v3/api-docs, which is where its
		generated client stubs (gebo.microservices.clients.parent) read the spec
		from. -->
		<profile>
			<id>swagger-on</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.springframework.boot</groupId>
						<artifactId>spring-boot-maven-plugin</artifactId>
						<configuration>
							<jvmArguments>
								--add-opens=java.base/java.nio.charset=ALL-UNNAMED</jvmArguments>
							<mainClass>ai.gebo.microservices.googledrive.GoogleDriveApplication</mainClass>
							<excludeDevtools>false</excludeDevtools>
							<layout>JAR</layout>
							<classifier>bootable+swagger</classifier>
						</configuration>
					</plugin>
				</plugins>
			</build>
			<dependencies>
				<dependency>
					<groupId>ai.gebo.architecture</groupId>
					<artifactId>gebo.architecture.swagger</artifactId>
					<version>${project.version}</version>
				</dependency>
			</dependencies>
		</profile>
		<profile>
			<id>bootables</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>exec-maven-plugin</artifactId>
						<configuration>
							<mainClass>ai.gebo.microservices.googledrive.GoogleDriveApplication</mainClass>
						</configuration>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-jar-plugin</artifactId>
						<configuration>
							<archive>
								<manifest>
									<mainClass>ai.gebo.microservices.googledrive.GoogleDriveApplication</mainClass>
								</manifest>
							</archive>
						</configuration>
					</plugin>
					<plugin>
						<groupId>org.springframework.boot</groupId>
						<artifactId>spring-boot-maven-plugin</artifactId>
						<configuration>
							<jvmArguments>
								--add-opens=java.base/java.nio.charset=ALL-UNNAMED</jvmArguments>
							<mainClass>ai.gebo.microservices.googledrive.GoogleDriveApplication</mainClass>
							<excludeDevtools>false</excludeDevtools>
							<layout>JAR</layout>
							<classifier>bootable</classifier>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>production</id>
			<properties>
				<!-- Critical -->
				<dependency-check.failBuildOnCVSS>9.0</dependency-check.failBuildOnCVSS>
				<!-- se lo scanner non funziona, produzione bloccata -->
				<dependency-check.failOnError>true</dependency-check.failOnError>
			</properties>
		</profile>
	</profiles>
</project>

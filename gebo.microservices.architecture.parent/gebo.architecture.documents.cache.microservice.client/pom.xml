<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>ai.gebo.microservices.architecture</groupId>
		<artifactId>gebo.microservices.architecture.parent</artifactId>
		<version>1.0.2.2-SNAPSHOT</version>
	</parent>
	<artifactId>gebo.architecture.documents.cache.microservice.client</artifactId>

	<!--
		WebClient-based client that implements IDocumentsCacheService and
		IDocumentsChunkService by calling the REST controllers exposed by the
		chunker microservice (gebo.architecture.documents.cache.microservice.controller).
		Base URL, api-key header and arbitrary extra headers are configurable under
		the ai.gebo.documents.cache.client.* properties. Drop this module on the
		classpath of a service that needs those beans but does not host the
		documents-cache implementation (e.g. brain.gebo.ai).
	-->
	<dependencies>
		<!-- Brings the two service interfaces + all model types (ChunkingParams,
		     DocumentChunkingResponse, IDocumentChunkWithRef, DocumentChunk,
		     StreamingPurpose, TypedInputStream, GDocumentReference, SearchResult, ...)
		     and, transitively, spring-webflux (WebClient) + reactor. -->
		<dependency>
			<groupId>ai.gebo.architecture</groupId>
			<artifactId>gebo.architecture.documents.cache</artifactId>
			<version>${project.version}</version>
		</dependency>
		<!-- IGDocumentContentStreamer + DocumentContentStreamerException: implemented
		     by DocumentContentStreamerWithCacheRestClient, which streams a document
		     through the chunker's local cache instead of its owning content handler. -->
		<dependency>
			<groupId>ai.gebo.architecture</groupId>
			<artifactId>gebo.architecture.documents.access</artifactId>
			<version>${project.version}</version>
		</dependency>
		<!-- GeboMicroserviceUrlResolver + its autoconfigured bean: resolves the
		     chunker microservice's base url. -->
		<dependency>
			<groupId>ai.gebo.microservices.architecture</groupId>
			<artifactId>gebo.microservices.topology</artifactId>
			<version>${project.version}</version>
		</dependency>
		<!-- @ConfigurationProperties / @ConditionalOnMissingBean support. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-autoconfigure</artifactId>
		</dependency>
	</dependencies>
</project>

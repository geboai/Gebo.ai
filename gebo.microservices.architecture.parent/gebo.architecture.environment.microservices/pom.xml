<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>ai.gebo.microservices.architecture</groupId>
    <artifactId>gebo.microservices.architecture.parent</artifactId>
    <version>1.0.3.0-SNAPSHOT</version>
  </parent>
  <artifactId>gebo.architecture.environment.microservices</artifactId>
  <dependencies>
		<dependency>
			<groupId>ai.gebo.architecture</groupId>
			<artifactId>gebo.architecture.environment</artifactId>
			<version>${project.version}</version>
		</dependency>
		<!-- The live microservices topology (yml-bound GeboMicroservicesTopology +
		     the GeboStandardMicroservices catalogue): the source
		     GClientsTopologyProviderImpl derives every published client relative
		     web context url from, so the clients topology and the gateway routing
		     table are always the same map read twice. -->
		<dependency>
			<groupId>ai.gebo.microservices.architecture</groupId>
			<artifactId>gebo.microservices.topology</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>
</project>
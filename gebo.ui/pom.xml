<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>ai.gebo</groupId>
		<artifactId>ai.gebo.parent</artifactId>
		<version>1.0.2.1</version>
	</parent>
	<artifactId>gebo.ui</artifactId>
	<properties>
		<swagger.file>http://localhost:12999/v3/api-docs</swagger.file>
		<backoffice.swagger.file>
			${project.basedir}/Backoffice.Swagger/swagger.json</backoffice.swagger.file>
		<generated-sources-path>
			${project.basedir}/projects/gebo-ai-rest-api/src/lib</generated-sources-path>
		<generated-sources-java-path>/projects/gebo-ai-rest-api/src/lib</generated-sources-java-path>
	</properties>
	<packaging>jar</packaging>
	<dependencies>
		<dependency>
			<groupId>ai.gebo.architecture</groupId>
			<artifactId>gebo.multilanguage.support</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

	</dependencies>


	<profiles>
		<profile>
			<id>angular-ui</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<build>
				<resources>
					<resource>
						<directory>${project.basedir}/dist/gebo-ai-ui/browser</directory>
						<targetPath>static</targetPath>
					</resource>
				</resources>
				<plugins>

					<plugin>
						<groupId>com.github.eirslett</groupId>
						<artifactId>frontend-maven-plugin</artifactId>
						<version>1.7.6</version>
						<configuration>
							<workingDirectory>./</workingDirectory>
							<nodeVersion>v22.14.0</nodeVersion>
							<npmVersion>10.9.2</npmVersion>
						</configuration>
						<executions>
							<execution>
								<id>install node and npm</id>
								<goals>
									<goal>install-node-and-npm</goal>
								</goals>
							</execution>
							<execution>
								<id>npm install --force</id>
								<goals>
									<goal>npm</goal>
								</goals>
							</execution>
							<execution>
								<id>npm run export-libs</id>
								<goals>
									<goal>npm</goal>
								</goals>
								<configuration>
									<arguments>run export-libs</arguments>
								</configuration>
							</execution>
							<execution>
								<id>npm run build-prod</id>
								<goals>
									<goal>npm</goal>
								</goals>
								<configuration>
									<arguments>run build-prod</arguments>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>generate-rest-api</id>
			<build>
				<plugins>
					<plugin>
						<groupId>io.swagger.codegen.v3</groupId>
						<artifactId>swagger-codegen-maven-plugin</artifactId>
						<version>3.0.59</version>
						<configuration>
							<inputSpec>${swagger.file}</inputSpec>
							<!-- Overrides only encoder.mustache; every other file falls back to
							     swagger-codegen's built-in template. The stock encoder targets
							     Angular 10 (ngVersion below) and emits CustomHttpUrlEncodingCodec
							     WITHOUT the 'override' modifiers this project's TypeScript
							     requires, so a regeneration used to overwrite the committed
							     encoder.ts with code that cannot compile (error TS4114) - it only
							     ever built because the file had been hand-patched afterwards.
							     Shared with the 18 microservice stubs under
							     gebo.api.clients/gebo.microservices.clients.parent. -->
							<templateDirectory>${project.basedir}/../gebo.api.clients/swagger-codegen-templates/typescript-angular</templateDirectory>
							<configOptions>
								<sourceFolder>${generated-sources-java-path}</sourceFolder>
							</configOptions>
							<output>${generated-sources-path}</output>
						</configuration>
						<executions>
							<execution>
								<id>generate-swagger-angular</id>
								<phase>generate-sources</phase>
								<goals>
									<goal>generate</goal>
								</goals>
								<configuration>
									<language>typescript-angular</language>
									<modelPackage>model</modelPackage>
									<apiPackage>api</apiPackage>
									<invokerPackage>invoker</invokerPackage>
									<additionalProperties>
										ngVersion=10.0.6,libraryName=gebo.ai.client.rest</additionalProperties>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>

	</profiles>
</project>
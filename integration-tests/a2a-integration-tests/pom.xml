<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>ai.gebo.integration-tests</groupId>
    <artifactId>gebo.integration-tests.parent</artifactId>
    <version>1.0.3.0-SNAPSHOT</version>
  </parent>
  <artifactId>a2a-integration-tests</artifactId>

  <dependencies>
    <dependency>
      <groupId>ai.gebo.architecture</groupId>
      <artifactId>gebo.architecture.integration.tests</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>ai.gebo.apps</groupId>
      <artifactId>gebo.ai.app</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-qdrant</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>ai.gebo.clients</groupId>
      <artifactId>gebo.monolithic.api.resttemplate.client</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- A2A wire types (AgentCard, JsonUtil, JSON-RPC wrappers) to assert the
         served Agent Card and drive the mock A2A peer. -->
    <dependency>
      <groupId>ai.gebo.architecture</groupId>
      <artifactId>gebo.architecture.a2a-server</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>

<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>ai.gebo.llms</groupId>
		<artifactId>gebo.llms.parent</artifactId>
		<version>1.0.2.2</version>
	</parent>
	<artifactId>gebo.llms.openai</artifactId>
	<dependencies>
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-openai</artifactId>
			<version>${spring.ai.version}</version>
			<exclusions>
				<!-- com.openai:openai-java-core pulls the pre-jakarta
				     io.swagger.core.v3:swagger-annotations, which defines the
				     same io.swagger.v3.oas.annotations.* classes as
				     swagger-annotations-jakarta (brought by spring-ai-client-chat,
				     via gebo.architecture.llms.abstraction.layer) but is missing
				     newer methods (e.g. Schema.$dynamicRef()) springdoc calls at
				     runtime - a NoSuchMethodError, not a compile error, and only
				     on whichever classpath ordering happens to load the old jar's
				     class first. Exclude it; nothing here needs the non-jakarta
				     swagger annotations directly. -->
				<exclusion>
					<groupId>io.swagger.core.v3</groupId>
					<artifactId>swagger-annotations</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<dependency>
			<groupId>ai.gebo.llms</groupId>
			<artifactId>gebo.llms.openai.api.utils</artifactId>
			<version>${project.version}</version>
		</dependency>
		
	</dependencies>
</project>
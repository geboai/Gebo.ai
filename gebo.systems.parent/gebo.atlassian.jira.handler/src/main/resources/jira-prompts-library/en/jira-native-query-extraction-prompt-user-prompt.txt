INPUTS:
- Last User question:

{question}

- Chat history (recent, latest first, may be empty):

{latestInteractions}

- Consolidated previous chat history (may be empty):

{consolidatedChatHistory}

-(CAT) Jira projects catalog (may be empty):

{jiraProjects}


TASK:
1) Understand what Jira issues the user wants to find (projects, issue types, keys, status, priority, labels, versions).
2) Extract text search terms for summary and description (keywords/phrases) at least from 3 to 12 keywords/phrases.
3) Extract people constraints (assignee, reporter, creator) if explicitly or strongly implied.
4) If you recognise a project in the jira projects catalog (CAT) use it to restrict the searches. 
5) Choose match modes:
   - Use ANY when the user provides alternatives (e.g., “A or B”, “either”, multiple synonyms).
   - Use ALL when the user requires all terms (e.g., “must contain both”, “include X and Y”, tight filtering).
   - If not specified, default to ANY for text terms and labels (broader, safer recall).
6) Only fill fields you are confident about; otherwise leave them null (do not guess).
7) Keep lists small and precise (max ~10 items per list). Prefer exact project keys and issue keys if present.
8) If the user asks something NOT related to searching Jira issues, output an empty filter object (all null).
9) If the user asks for all tickets in a project (in all languages) and you discover the project recognizing its code 
   in (CAT) Jira projects catalog, include in the output structure the project code (field projectCodes) in the output
   data structure and empty values in all other fields. 

{format}

IMPORTANT CONSTRAINTS:
- Output MUST be a single JSON object, no markdown, no code fences, no extra text.
- Use Jira project keys / issue type names / status names as strings exactly as the user wrote them when available.
- For user fields, if the user indicates “me/my”, use: "currentUser()".
- Do not invent project keys, issue types, statuses, labels, or version names.
- Do not include JQL operators inside terms. Terms are plain phrases (e.g., "timeout", "payment gateway").
- If the user provides an issue key pattern like ABC-123, put it in issueKeys.